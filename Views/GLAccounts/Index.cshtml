@model IEnumerable<RabApiWithMvc.Models.GLAccount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GlCreditor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GLDebit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsDeleted)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GlCreditor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GLDebit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateCreated)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsDeleted)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GLAccId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GLAccId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GLAccId })
        </td>
    </tr>
}

</table>
